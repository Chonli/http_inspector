name: http_inspector
description: 'A package to inspect http requests and responses in Flutter directly in your App.'
version: 0.0.1

environment:
  sdk: ^3.10.0
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  http: ^1.6.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.10.4
  freezed: ^3.2.3
